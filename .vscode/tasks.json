{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build dsa",
      "type": "process",
      "command": "g++",
      "args": [
        "-std=c++17",
        "-mconsole",
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}.exe"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"]
    }
  ]
}
